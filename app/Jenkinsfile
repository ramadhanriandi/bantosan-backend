pipeline {
    agent {
        label "dockerworker"
    }
    stages {
        stage('Build') {
            steps {
                dir('app') {
                    sh 'mvn -B -DskipTests clean package'
                }
            }
        }
        // stage('Test') {
        //     steps {
        //         dir('app') {
        //             sh 'mvn test'
        //         }
        //     }
        //     post {
        //         always {
        //             junit 'target/surefire-reports/*.xml'
        //         }
        //     }
        // }
        stage('Build image') {
            steps {
                sh 'docker build -t bantosan .'
            }
        }
        stage('Run app') {
            steps {
                sh 'docker run bantosan'
            }
        }
    }
    post {
        always {
            deleteDir()
        }
    }
}